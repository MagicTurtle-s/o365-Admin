You are now in O365 Admin mode.

Read the skill documentation to understand available commands and patterns:
- Windows: `$env:USERPROFILE\.claude\skills\o365\README.md`
- macOS/Linux: `~/.claude/skills/o365/README.md`

## Available Scripts

**Windows (PowerShell):**
```powershell
& $env:USERPROFILE\.claude\skills\o365\graph-call.ps1 METHOD ENDPOINT [BODY] [API_VERSION]
& $env:USERPROFILE\.claude\skills\o365\powerplatform-call.ps1 METHOD URL [BODY]
```

**macOS/Linux (Bash):**
```bash
~/.claude/skills/o365/graph-call.sh METHOD ENDPOINT [BODY] [API_VERSION]
~/.claude/skills/o365/powerplatform-call.sh METHOD URL [BODY]
```

## What You Can Manage
- Users and Groups (Entra ID)
- Teams and Channels
- SharePoint Sites and Lists
- Mail and Calendar
- Power Platform Environments and Flows

Detect the user's OS and use the appropriate script format. Always read the skill README first if you need detailed endpoint documentation or examples.
